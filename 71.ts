const pi=3.14;
let radius=10;
console.log("Radius value is "+radius);
console.log("Pi value is "+pi);
console.log("changing the values");
/**
 * We can't change the values of pi because it is a constant variable
*/
// try{
// pi=12;
// }
// catch(e){
//     console.log("We can't change the values of pi because it is a constant variable"+e);
// }
//Radius is not a constant variable so we can change it
radius=20;
console.log("Radius value is changed to "+radius);
console.log("Pi value is can't be change because it is constant "+pi);




